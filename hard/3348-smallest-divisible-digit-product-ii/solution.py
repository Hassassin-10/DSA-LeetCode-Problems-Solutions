            rem[i + 1] = rem[i] // math.gcd(rem[i], int(num_list[i]))
                break
                pos = i
            if num_list[i] == "0":
        num_list = list(num)
        for i in range(n):

        pos = n - 1
        rem[0] = t
        rem = [0] * (n + 1)

        n = len(num)
            return "-1"
        if temp > 1:

                temp //= i
            while temp % i == 0:
        for i in range(2, 10):
        temp = t
    def smallestNumber(self, num: str, t: int) -> str:
class Solution:
